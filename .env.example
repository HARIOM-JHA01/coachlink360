# Resend API Key (get from https://resend.com/api-keys)
RESEND_API_KEY=re_your_api_key_here

# Email settings
FROM_EMAIL=onboarding@resend.dev
FROM_NAME=CoachLink360

# Database connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/coachlink360

# Application settings
PORT=3000
NODE_ENV=development

# Base URL for survey links (update in production)
BASE_URL=http://localhost:3001

# Optional admin token to protect admin endpoints
# If set, include header `x-admin-token: <token>` or ?token=<token> on /admin/responses and /api/admin/responses
ADMIN_TOKEN=your_admin_token_here
